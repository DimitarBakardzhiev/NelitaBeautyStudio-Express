extends ../layout

block content
    .container
        .col-sm-6.col-sm-offset-3
            h1
                span.fa.fa-sign-in
                |  Вход
            if message.length > 0
                .alert.alert-danger= message

            form(action='/auth/login', method='post')
                .form-group
                    label Email
                    input.form-control(type='email', name='email', autofocus, required)
                .form-group
                    label Парола
                    input.form-control(type='password', name='password', required)
                button.btn.btn-warning.btn-lg(type='submit') Влез
            hr
            p
                | Нямаш регистрация?
                a(href='/auth/signup')  Регистрирай се тук.